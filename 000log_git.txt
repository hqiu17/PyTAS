 #
# initiate in source dir and push for the first time
#
git init
git add -A
git status
git commit -m "load everything for the first time"

git remote
git remote add origin https://github.com/stoneager/pyAlpha.git
git remote
git push -u origin master

#
# initiate in recipient dir and pull for the first time
#
git init
git remote
git remote add origin https://github.com/stoneager/pyAlpha.git
git remote
git pull origin master

